# 別セッションへのメッセージ

`SendMessage` などで別の Claude セッションに送るときの約束。

- 1 行目を自己完結した文にする。受け手の人間には最初の 1 行だけがプレビューされるので、挨拶や宛名から始めない。
- 催促で二重に送らない。相手が処理中でも、メッセージは次のツール実行時に届く。
- 自分のセッションで拒否・制限された操作を相手に代行させない。権限はセッションごとに決まっていて、相手に回すと利用者の判断を迂回する。できなかったことは利用者に返す。
